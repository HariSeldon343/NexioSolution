# Piattaforma Collaborativa - Configurazione Apache

# Abilita il rewrite engine
RewriteEngine On

# Imposta la directory base
RewriteBase /piattaforma-collaborativa/

# Sicurezza: Proteggi file sensibili
<FilesMatch "\.(env|json|lock|sql|md)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Proteggi directory sensibili
RedirectMatch 404 /\.git
# Commentiamo temporaneamente per debug - /backend/config/ contiene file PHP necessari
# RedirectMatch 404 /backend/config/
RedirectMatch 404 /database/
RedirectMatch 404 /logs/

# Previeni directory listing
Options -Indexes

# Imposta charset UTF-8
AddDefaultCharset UTF-8

# Compressione GZIP
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/json
</IfModule>

# Cache control
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>

# Sicurezza headers
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
    Header set Access-Control-Allow-Origin "*"
    Header set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
    Header set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
    Header set Access-Control-Allow-Credentials "true"
</IfModule>

# Limita dimensione upload
LimitRequestBody 10485760

# Gestione errori personalizzata
ErrorDocument 403 /403.php
ErrorDocument 404 /404.php
ErrorDocument 500 /500.php

# Rewrite rules per URL puliti
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/(.*)$ backend/api/$1 [L,QSA]

# Rimuovi .php dalle URL (opzionale)
# Commentiamo temporaneamente - potrebbe causare loop
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteRule ^([^\.]+)$ $1.php [NC,L]

# Forza HTTPS in produzione (decommentare se necessario)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# PHP settings (se permesso dall'hosting)
# Commentiamo temporaneamente per debug - potrebbero causare errori 500
# <IfModule mod_php7.c>
#     php_value upload_max_filesize 10M
#     php_value post_max_size 10M
#     php_value max_execution_time 300
#     php_value max_input_time 300
#     php_value memory_limit 128M
#     php_flag display_errors Off
#     php_flag log_errors On
# </IfModule>

# PHP settings
# <IfModule mod_php.c>
#     php_value upload_max_filesize 50M
#     php_value post_max_size 50M
#     php_value max_execution_time 300
#     php_value max_input_time 300
# </IfModule>

# Rewrite rules
<IfModule mod_rewrite.c>
    RewriteCond %{HTTP:X-Forwarded-For} ^172\.
    RewriteRule .* - [E=ONLYOFFICE:1]
</IfModule> 